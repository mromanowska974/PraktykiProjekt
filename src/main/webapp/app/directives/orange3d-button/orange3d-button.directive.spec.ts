import { Orange3dButtonDirective } from './orange3d-button.directive';

describe('Orange3dButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new Orange3dButtonDirective();
    expect(directive).toBeTruthy();
  });
});
